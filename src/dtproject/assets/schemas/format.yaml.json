{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$comment": "Schema for /dtproject/.dtproject.yaml file",
    "type": "object",
    "properties": {
        "version": {
            "type": "integer",
            "description": "Version of the project format"
        }
    },
    "required": ["version"],
    "additionalProperties": false
}